import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  wrapper: {
    width: 'fit-content',
    marginLeft: 50,
  },
  tooltipSlider: {
    width: 200,
    marginTop: 10,
    marginBottom: 10,
  },
}));

export default useStyles;
