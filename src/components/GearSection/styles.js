import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  wrapper: {
    margin: '0.85rem',
  },
  cardWrapper: {
    margin: '1.5rem',
  },
}));

export default useStyles;
